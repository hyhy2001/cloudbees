
# Ablation harness output (regenerated by scripts/ablation.ts)
ablation-report.md
# agent instruction files (AI-generated, project-local)
AGENTS.md
.atlas/
bee.lm.json
.bee_secret
benchmark-report.md
.bun/
.claude/
.claudeignore
CLAUDE.md
# Comprehensive benchmark output (regenerated by scripts/benchmark.ts)
data/.bee_secret
data/*.db
data/*.db-shm
data/*.db-wal
dist/
legacy/
# LM endpoint config baked into the binary at build time (contains API key)
# local tooling state (not part of the project)
makefile_vars
node_modules/
# project-local bun toolchain (bootstrapped by `make`)
# RAG eval harness output (regenerated by scripts/rag-eval.ts)
rag-eval-report.md
# session secret (never commit)
src/generated/embedding-model.ts
*.tsbuildinfo
bee.csh
