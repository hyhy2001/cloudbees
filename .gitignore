node_modules/
dist/
legacy/
makefile_vars
data/*.db
*.tsbuildinfo

# session secret (never commit)
data/.bee_secret
.bee_secret

# local tooling state (not part of the project)
.claude/
.atlas/
CLAUDE.md

# project-local bun toolchain (bootstrapped by `make`)
.bun/
